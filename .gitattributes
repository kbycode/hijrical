# Normalize line endings to LF in the repository.
* text=auto

*.py    text
*.yml   text
*.toml  text
*.md    text
*.cfg   text
*.txt   text

# Treat built artifacts as binary (should be gitignored anyway).
*.whl    binary
*.tar.gz binary
